import './core'

// App-specific models go here. core.zmodel already provides User/File/RefreshToken plus
// the Role + RolePermission based RBAC (see core.zmodel's AuthInfo/Role/RolePermission) -
// use `auth().role` and RolePermission-backed `auth().permissions` in `@@allow`/`@@deny`
// policies the same way, not a client-side-only check.
